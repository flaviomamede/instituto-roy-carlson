# Arquivos do sistema
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Arquivos temporários
*.tmp
*.temp
*.log
*.bak
*.swp
*.swo
*~

# Arquivos de IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Arquivos de build
build/
dist/
out/
*.min.js
*.min.css

# Dependências
node_modules/
vendor/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Arquivos de cache
.cache/
.parcel-cache/
.next/
.nuxt/

# Arquivos de documentação temporários
*.aux
*.fdb_latexmk
*.fls
*.synctex.gz
*.toc
*.out
*.nav
*.snm
*.vrb

# Arquivos específicos do projeto

# === Conteúdo sensível / privado (ATENÇÃO: este repositório é PÚBLICO) ===
# Acervo exclusivo servido somente via API — nunca versionar os PDFs privados
site/api/_data/library/files/
# Lista de e-mails autorizados (fallback de dev). Em produção usar IRC_ALLOWLIST_JSON
biblioteca-adapters/irc/allowlist.json
# Material-fonte bruto do acervo (PDFs/livros — muitos privados e/ou autorais)
/biblioteca/
# Segredos
.env*
!.env.example
!**/.env.example

# === Backups, protótipos e fontes de staging (não versionar) ===
site-instituto-roy-carlson.zip
prototipo-site-irc.zip
/prototipo-site-irc/
/site-old/
/_versoes/
/_arquivo/
/reunioes/
# Fontes de staging (os artefatos publicados ficam em site/)
/revista/
/imagens/
# Livros do Protz retidos para revisão de direitos (não publicar)
biblioteca-adapters/irc/catalog.protz-held.json
biblioteca-adapters/irc/extra-files.protz-held.tsv
